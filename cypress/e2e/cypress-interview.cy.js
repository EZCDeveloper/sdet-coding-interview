/// <reference types="cypress" />

describe("Xepelin Interview", () => {
  beforeEach(() => {
    // Navegar a la pagina: "https://google.com"
  });

  it("Search on Google page", () => {
    // 1. Definir los 3 elementos del google-page.ts en la carpeta fixtures/page-objects
    // 2. Importar el page-object Google Page
    // 3. SearchTextBox verificar que sea visible e insertar "Xepelin" en el textbox
    // 4. SearchOption clickear
    // 5. XepelinLabelText debe tener el siguiente texto:
    // "Financiamos pago a proveedores y adelantamos el cobro de facturas. Solicita tu Crédito en línea, Recibe una Tasa Personalizada y Respuesta en minutos."
  });
});
