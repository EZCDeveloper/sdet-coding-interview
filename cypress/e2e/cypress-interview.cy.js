/// <reference types="cypress" />

describe("Xepelin Interview", () => {
  beforeEach(() => {
    // 0. Navegar a la pagina: "https://google.com"
  });

  it("Search on Google page", () => {
    // 1. Insertar los locators de los 3 elementos que se encuentra en el archivo “google-page.ts” en la carpeta fixtures/page-objects

    // 2. Importar el page-object Google Page
    
    // 3. Usar el elemento "SearchTextBox" para verificar que sea visible e insertar un texto "Xepelin" en el campo
    
    // 4. Usar el elemento "SearchOption" para clickear
    
    // 5. Usar el elemento "XepelinLabelText" y validar que tenga el siguiente texto:
    // "Financiamos pago a proveedores y adelantamos el cobro de facturas. Solicita tu Crédito en línea, Recibe una Tasa Personalizada y Respuesta en minutos."
  });
});
